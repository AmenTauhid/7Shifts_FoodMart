import Foundation

enum APIEndpoints {
    static let baseURL = "https://7shifts.github.io/mobile-takehome/api"

    static let foodItems = "\(baseURL)/food_items.json"
    static let categories = "\(baseURL)/food_item_categories.json"
}
